.span6.offset3
  %h2 Details about #{@movie.title}

  %ul.unstyled
    %li
      Rating:
      = @movie.rating
    %li
      Released on:
      = @movie.release_date.strftime("%B %d, %Y")

  %h3 Description
  %p= @movie.description

  =link_to "Back to movie list", movies_path