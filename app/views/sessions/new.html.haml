.col-lg-6.col-offset-3
  %h2.page-header Log In
  - if @current_user
    %p= "You are currently logged in as #{@current_user.name}"
    = link_to "Log out", logout_path, class: "btn btn-default"
  - else
    = link_to "Log in with Twitter", auth_path, class: "btn btn-primary"
