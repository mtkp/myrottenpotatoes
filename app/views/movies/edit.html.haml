.col-md-6.col-md-offset-3
  %h3 Edit Movie

  = form_tag movie_path(@movie), method: :put do
    = render 'movie_form', movie: @movie
    = submit_tag "Save Changes", class: "btn btn-primary"
    = link_to "Cancel", movie_path(@movie), class: "btn btn-default"
